# TS Runtime Bundle (No Data)

This bundle contains all non-data files needed to run the TypeScript MCP workflow.

Run from this directory:

```bash
node backend/scripts/run_patient_evolution_mcp.js --identifier <id> --data-root /absolute/path/to/data
```
